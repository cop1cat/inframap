apiVersion: v1
kind: Service
metadata:
  name: {{ include "inframap-viewer.fullname" . }}
  labels:
    {{- include "inframap-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inframap-viewer.selectorLabels" . | nindent 4 }}
